class AccessExep implements Exception {
  String cause;

  AccessExep(this.cause);
}

class ConnectionExep implements Exception {
  String cause;

  ConnectionExep(this.cause);
}

class ErrorsExep implements Exception {
  String cause;

  ErrorsExep(this.cause);
}

class WorningExep implements Exception {
  String cause;

  WorningExep(this.cause);
}
